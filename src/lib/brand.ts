/**
 * Identidade do produto em um só lugar.
 *
 * Nome e logo aparecem na sidebar, no login, na troca de senha e no título da
 * aba. Centralizados aqui para que trocar a marca seja mexer neste arquivo e
 * substituir `public/logo.png` — não caçar string espalhada por tela.
 */

export const APP_NAME = "JtechDash";

/** Linha de apoio sob o nome, no login. */
export const APP_TAGLINE = "Gestão multi-marketplace";

/** Arquivo em `public/`. Trocar a imagem lá troca a logo em todo o sistema. */
export const LOGO_SRC = "/logo.png";
